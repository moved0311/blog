# 2026-06-20
[X] 1. Do DB questions or Learn a DB topic
	- CS50 SQL - Lecture 6 - Scaling
[X] 2. self-introduction page
[X] 3. Leetcode daily / Rating 1700以下的題目練習
	- 二次掃描(135, 3796) 
[ ] Do one system design question
[X] Learning English
	- work on
	- admittedly: 不可否認地、必須承認的是
  	- Admittedly, my English is not perfect.

CS50 SQL - Lecture 6 - Scaling

前面課程都是用SQLITE實現，現在換到SQL介紹資料類別差異

Integers

| data type | size(bytes) |
| --------- | ----------- |
| TINYINT   | 1           |
| SMALLINT  | 2           |
| MEDIUMINT | 3           |
| INT       | 4           |
| BIGINT    | 8           |

```sql
CREATE TALBE `card` (
	`id` INT AUTO_INCREMENT,
	PRIMARY KEY(`id`)
);

SHOW TABLES;
DESCRIBLE `card`;
```

STRING
- CHAR(M)       固定長度
- VARCHAR(M)    變動長度
- TEXT (TINYTEXT/TEXT/MEDIUMTEXT/LONGTEXT)
- BLOB
- ENUM
- SET

```sql
CREATE TABLE `stations` (
	`id` INT AUTO_INCREMNET,
	`name` VARCHAR(32) NOT NULL UNIQUE,
	`line` ENUM('blue','green','oragne','red') NOT NULL,
	PRIMARY KEY(`id`)
);
```

Dates / Times
- DATE
- TIME
- DATETIME
- TIMESTAMP
- YEAR

Real Numbers
- FLOAT 4
- DOUBLE PRECISION 8

```sql
CREATE TABLE `swipes` (
	`id` INT AUTO_INCREMNET,
	`card_id` INT,
	`station_id` INT,
	`type` ENUM('enter', 'exit', 'deposit') NOT NULL,
	`datetime` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
	`amount` DECIMAL(5, 2) NOT NULL CHECK(`amount` != 0),
	PRIMARY KEY(`id`),
	FOREIGN KEY(`card_id`) REFERENCE `cards`(`id`),
	FOREIGN KEY(`station_id`) REFERENCE `stations`(`id`)
);
```

Altering table

```sql
ALTER TABLE `station`
MODIFY `line` ENUM('blue','green','orange', 'red', 'sliver') NOT NULL

ALTER TALBE `collections`
ADD COLUMN `delted` TINYINT DEFALUT 0;
```

Stored Procedures

有些可重複的查詢/操作，可以做成function執行

```sql
CREATE PROCEDURE name
BEGIN
...
END;

CREATE PROCEDURE `current_collection`()
BEGIN
SELECT `title`, `accesssion_number`, `acquired`
FROM `collections` WHERE `deleted` = 0;
END;

CALL `current_collection`();
```

```sql
CREATE PROCEDURE `sell`(IN `sold_id` INT)
BEGIN
UPDATE `collections` SET `deleted` = 1
WHERE `id` = `sold_id`;
INSERT INTO `transations` (`title`,`action`)
VALUES((SELECT `title` FROM `collections` WHERE `id` = `sold_id`), 'sold')
END;

CALL `sell`(2);
```


Vertical Scaling
	- increasing capacity by increaseing a server's computing power

Horizontal Scaling
	- Increasing capacity by distributing load across multiple servers

Replication
	- Kepping copies of a database on multiple servers
  

Single-Leader
Multi-Leader
Leaderless

Single-Leader
	- Leader Server, Follower Server
	- Leader Server 負責寫入新資料，再更新到Follower Server

Read Replica
	- A copy of a database from which data may only be read

Synchronous
Asychronous


Sharding
	把一個大的資料庫拆分到多個小的資料庫

Single Point of Failure(SPOF)


SQLite沒有權限的東西
但在MYSQL有，一般新建立的使用者看不到DB中的table，
需要admin增加權限

```sql
GRANT privilege, ... TO user;
REVOKE privilege, ... FROM user;
-- privilege: ALL, CREATE, INSERT, SELECT, UPDATE, DELETE,...

GRANT SELECT ON `dbname`.`tablename` TO username;
```


SQL injection
```sql
-- PREPARE name FROM statment;

PREPARE `balance_check`
FROM 'SELECT * FROM `accounts` WHERE `id` = ?';

SET @id = 1;
EXECUTE `balance_check` USING @id;

SET @id = '1 UNION SELECT * FROM `accounts`';
EXECUTE `balance_check` USING @id;
```