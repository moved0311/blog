---
title: Command Line
category: note
date: 2024-09-05
---

## Move

- `cd -` : go to previous directory

## Type
- `C-u` : delete all line 
- `C-w` : delete word

## Search 搜尋

- ctrl + r `type keyword to search history command`
- `!! <Enter>`
- grep -r "keyword" .


## Kill process running port

```sh
lsof -i:3000
kill <PID>
```

## 目錄大小

```sh
du -sh <path>
```

## Library
### ripgrep

`brew install ripgrep`

usage:

```sh
rg <word>
```

## wget
`wget {url}` 把檔案抓下來


## netcat
TCP/IP 瑞士刀 (Swiss Army Knife)

```sh
nc <host> <port>
```

## strings
把binary file -> string

## 讀檔
```sh
open('dictionary.txt', 'rb').read()
open('dictionary.txt', 'r').read().splitlines()
open('dictionary.txt', 'rb').read().decode('utf-8')
```
