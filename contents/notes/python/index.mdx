---
draft: true
title: Python
date: 2024-09-29
---

## a-z
```py
ascii_lowercase #"abcdefghijklmnopqrstuvwxyz"
```

```py
ord('a') # 97
ord('b') # 98

chr(97) # a
chr(98) # b
```

## Bit
```py
(5).bit_length() # 3
(8).bit_length() # 4
```

```py
(5).bit_count() # 2
(8).bit_count() # 1
```


## Counter
```py
f = collections.Counter()
s = "aeioqq"

for c in s:
	f[c] += 1

# Counter({'a': 1, 'b': 1, 'c': 1, 'e': 1, 'd': 1, 'i': 1, 'o': 1, 'u': 1})
```
